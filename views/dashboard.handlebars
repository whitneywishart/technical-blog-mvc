<div>
    <a href="/dashboard/create/"><button id="create-post" class="btn btn-primary">Create post</button></a>
    <h2>Dashboard</h2>
    {{#if posts.length}}

</div>

{{#each posts as |post|}}
{{> post-info post}}

<a href="/dashboard/edit/{{post.id}}"><button class="btn btn-primary">Edit</button></a>
</div>
{{/each}}

{{else}}
<p>The posts you create will appear here.</p>


{{/if}}