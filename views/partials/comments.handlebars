<div class="comments">
  {{#each this}}
  <div class="comment">
    <div class="text">
      {{comment_text}}
    </div>
    <div class="mt-3">
     &#8212;{{user.username}}, {{format_date created_at}}
    </div>
  </div>
  {{/each}}
</div>